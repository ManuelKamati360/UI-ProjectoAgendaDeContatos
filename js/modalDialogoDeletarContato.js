// ------------------ Modal Deletar ------------------

function abrirModalDeletar(id) {
  const contato = contatos.find(c => c.id == id);
  console.log("Abrir modal de exclusão para:", contato);
  
  // aqui entra teu código real de modal
}
